
# this is a shell script, don't put spaces around '=' symbol
# it should be 'sourced' (== import all definition to your current shell) like this:
# source secrets.sh

export SECRET_KEY='not so secret...'
